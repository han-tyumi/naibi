{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/han-tyumi/naibi/schema/game.schema.json",
  "title": "Card Game",
  "description": "One card game entry in Naibi. Every field is written from scratch in the project's own words; no text is copied or closely paraphrased from any source.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "name",
    "aliases",
    "category",
    "players",
    "decks",
    "equipment",
    "setup",
    "play",
    "goal_and_scoring",
    "variants",
    "difficulty",
    "duration_minutes",
    "tags",
    "sources_consulted"
  ],
  "properties": {
    "id": {
      "description": "Slug identifying the game. Must match the filename (games/<id>.json).",
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "minLength": 2,
      "maxLength": 60
    },
    "name": {
      "description": "Display name of the game.",
      "type": "string",
      "minLength": 2,
      "maxLength": 80
    },
    "aliases": {
      "description": "Other names the game is commonly known by. May be empty.",
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 80
      },
      "uniqueItems": true
    },
    "category": {
      "description": "Primary family the game belongs to, chosen by its core mechanic.",
      "type": "string",
      "enum": [
        "trick-taking",
        "shedding",
        "rummy-type",
        "solitaire",
        "casino",
        "matching-collecting",
        "bluffing"
      ]
    },
    "players": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "min",
        "max",
        "ideal"
      ],
      "properties": {
        "min": {
          "type": "integer",
          "minimum": 1,
          "maximum": 12
        },
        "max": {
          "type": "integer",
          "minimum": 1,
          "maximum": 12
        },
        "ideal": {
          "description": "Single best player count. Ranges belong in the prose, not here.",
          "type": "integer",
          "minimum": 1,
          "maximum": 12
        }
      }
    },
    "decks": {
      "description": "What you need to deal, in plain English, e.g. \"1 standard deck (52 cards)\" or \"2 standard decks plus 4 jokers (108 cards)\". Written for a person to read; the machine-readable version is in \"equipment\".",
      "type": "string",
      "minLength": 5,
      "maxLength": 200
    },
    "equipment": {
      "description": "The same requirement as \"decks\", but as data, so an app can answer \"what can I play with what I own?\" without parsing prose.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "standard_decks",
        "jokers",
        "special_deck",
        "other"
      ],
      "properties": {
        "standard_decks": {
          "description": "How many ordinary 52-card decks you must OWN to play at the minimum player count. Games needing a stripped deck still count the packs they are built from: Euchre is 1, Pinochle is 2. Use 0 for a game that needs a purpose-built pack you cannot make from ordinary cards, such as hanafuda, and name it in special_deck.",
          "type": "integer",
          "minimum": 0,
          "maximum": 8
        },
        "jokers": {
          "description": "How many jokers the game requires. 0 when jokers are unused or optional.",
          "type": "integer",
          "minimum": 0,
          "maximum": 8
        },
        "special_deck": {
          "description": "Set when play uses a stripped or purpose-built pack rather than a full one, e.g. \"24-card euchre deck\". Null for ordinary play.",
          "type": [
            "string",
            "null"
          ],
          "minLength": 3,
          "maxLength": 120
        },
        "decks_by_players": {
          "description": "How many decks are needed from a given player count upward, e.g. {\"6\": 2} for a game that wants a second pack from six players. Read as: the value for the largest key at or below the table size, falling back to standard_decks. Omit when the requirement never changes. Use it for per-player games too -- Nertz needs one deck each, so every count gets an entry.",
          "type": "object",
          "propertyNames": {
            "pattern": "^[1-9][0-9]?$"
          },
          "additionalProperties": {
            "type": "integer",
            "minimum": 1,
            "maximum": 12
          },
          "minProperties": 1
        },
        "other": {
          "description": "Anything needed beyond cards, e.g. a cribbage board. Usually empty.",
          "type": "array",
          "maxItems": 4,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 3,
            "maxLength": 120
          }
        }
      }
    },
    "setup": {
      "description": "How the table starts: deal, layout, dealer selection, anything built before the first turn.",
      "type": "string",
      "minLength": 120,
      "maxLength": 3000
    },
    "layout": {
      "description": "The starting table as data, so a setup diagram can be drawn rather than illustrated by hand. Optional: include it where the arrangement is worth seeing, omit it where 'everyone holds cards' says everything (most pure trick-taking games). Rows are drawn top to bottom and each row is centred, which is what makes a pyramid a pyramid and a cross a cross.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "rows"
      ],
      "properties": {
        "caption": {
          "description": "One line under the diagram, for anything the shapes cannot say.",
          "type": "string",
          "minLength": 4,
          "maxLength": 200
        },
        "overlapping_rows": {
          "description": "How many rows from the top are drawn overlapping one another, for layouts where a card is physically covered by the cards below it — a pyramid, or TriPeaks. Leave unset (0) and every row is drawn clear of its neighbours, which is right for a grid or a clock face. Set it where the covering IS the rule, because a diagram that draws those cards apart contradicts the rule it is illustrating.",
          "type": "integer",
          "minimum": 0,
          "maximum": 8
        },
        "rows": {
          "type": "array",
          "minItems": 1,
          "maxItems": 8,
          "items": {
            "description": "One horizontal band of the table, drawn left to right and centred.",
            "type": "array",
            "minItems": 1,
            "maxItems": 13,
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "kind"
              ],
              "properties": {
                "kind": {
                  "description": "What the pile is for. Drives how it is drawn. \"gap\" is an empty spacer used to position the others.",
                  "type": "string",
                  "enum": [
                    "tableau",
                    "foundation",
                    "stock",
                    "waste",
                    "reserve",
                    "free-cell",
                    "hand",
                    "discard",
                    "meld",
                    "trick",
                    "gap"
                  ]
                },
                "label": {
                  "description": "Caption under the pile, e.g. \"Foundations\". Omit on gaps.",
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 40
                },
                "repeat": {
                  "description": "How many identical piles sit side by side. Defaults to 1.",
                  "type": "integer",
                  "minimum": 1,
                  "maximum": 13
                },
                "cards": {
                  "description": "Cards dealt to each pile at the start. One number for all of them, or one per pile when they differ (Klondike's tableau is [1,2,3,4,5,6,7]). Omit when the count varies with the player count or is not fixed.",
                  "oneOf": [
                    {
                      "type": "integer",
                      "minimum": 0,
                      "maximum": 52
                    },
                    {
                      "type": "array",
                      "minItems": 1,
                      "maxItems": 13,
                      "items": {
                        "type": "integer",
                        "minimum": 0,
                        "maximum": 52
                      }
                    }
                  ]
                },
                "face": {
                  "description": "How the cards sit. \"last-up\" is the Klondike case: face down with the final card turned up. Defaults to \"up\".",
                  "type": "string",
                  "enum": [
                    "up",
                    "down",
                    "last-up"
                  ]
                },
                "fan": {
                  "description": "Whether the pile is spread so every card shows, rather than squared into a stack. Defaults to true for a tableau and false for everything else, which is usually right — set it explicitly where it is not, such as a discard pile you are allowed to take from part-way down.",
                  "type": "boolean"
                }
              }
            }
          }
        }
      }
    },
    "play": {
      "description": "How a turn works and how the hand proceeds: turn order, legal moves, restrictions, how the hand ends. The ceiling is a guard against sprawl rather than a target — the median entry is around 3,000 characters. Contract Bridge sits near the top and has earned it; if a simpler game is pressing the limit, the entry probably needs editing rather than more room.",
      "type": "string",
      "minLength": 200,
      "maxLength": 5500
    },
    "figures": {
      "description": "Captioned rows of named cards, for the things prose explains badly: what order cards rank in, and what a valid combination actually looks like. Optional, and worth including only where the answer is not obvious -- a ranking strip for a game that ranks ace high teaches nothing.",
      "type": "array",
      "minItems": 1,
      "maxItems": 4,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "kind",
          "caption",
          "rows"
        ],
        "properties": {
          "kind": {
            "description": "\"ranking\" for cards in order of power, \"meld\" for a valid (or invalid) combination.",
            "type": "string",
            "enum": [
              "ranking",
              "meld"
            ]
          },
          "caption": {
            "description": "What the figure shows, e.g. \"Trump ranking when diamonds are trump\".",
            "type": "string",
            "minLength": 6,
            "maxLength": 200
          },
          "rows": {
            "type": "array",
            "minItems": 1,
            "maxItems": 4,
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "cards"
              ],
              "properties": {
                "label": {
                  "description": "Caption to the left of the row, e.g. \"Valid run\".",
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 40
                },
                "valid": {
                  "description": "Set false to mark a row as a counter-example, drawn struck through. Useful for the mistakes people actually make, like running king-ace-two.",
                  "type": "boolean"
                },
                "cards": {
                  "type": "array",
                  "minItems": 1,
                  "maxItems": 14,
                  "items": {
                    "type": "object",
                    "additionalProperties": false,
                    "required": [
                      "face"
                    ],
                    "properties": {
                      "face": {
                        "description": "The card as a player would say it: \"A♠\", \"10♦\", \"J♥\". Use the suit symbols. \"?\" for any card, \"Jkr\" for a joker.",
                        "type": "string",
                        "minLength": 1,
                        "maxLength": 4
                      },
                      "note": {
                        "description": "Small caption under this one card, e.g. \"Right bower\".",
                        "type": "string",
                        "minLength": 1,
                        "maxLength": 24
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    },
    "figure_refs": {
      "description": "Ids of shared figures from shared/figures.json to show on this game, appended after any inline `figures`. Use this where a figure is genuinely identical across several games — the poker hand rankings are the same in every poker variant — so an error gets corrected once rather than in every entry that repeats it. Readers still see the figure on every page; only the source is shared.",
      "type": "array",
      "minItems": 1,
      "maxItems": 6,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
      }
    },
    "goal_and_scoring": {
      "description": "What winning looks like and how points (if any) are counted.",
      "type": "string",
      "minLength": 120,
      "maxLength": 3000
    },
    "background": {
      "description": "Where the game comes from and anything else worth knowing that is not a rule: origin, age, how the name is used elsewhere, why it looks the way it does. Optional, and rendered AFTER the rules and the variants -- a reader with a deck in hand wants the deal, not the eighteenth century. Keep material that changes what someone does at the table in `setup` instead: that a game needs a pack you cannot substitute, or that one player is playing against the other two, is orientation rather than background.",
      "type": "string",
      "minLength": 80,
      "maxLength": 1500
    },
    "deal": {
      "description": "Hand size by player count, and anything taken out of the deck to make it come out even. Include it where this actually varies -- Hearts strips cards at 3, 5 and 6 players -- and omit it where one number covers every case, since \"thirteen each\" needs no table.",
      "type": "array",
      "minItems": 1,
      "maxItems": 10,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "players",
          "hand"
        ],
        "properties": {
          "players": {
            "description": "The player count this row applies to.",
            "type": "integer",
            "minimum": 1,
            "maximum": 12
          },
          "hand": {
            "description": "Cards dealt to each player. Use 0 where the whole deck is shared out and the count follows from the deck size.",
            "type": "integer",
            "minimum": 0,
            "maximum": 52
          },
          "removed": {
            "description": "Cards taken out of the deck at this player count, e.g. \"2♦\". Omit when none are.",
            "type": "string",
            "minLength": 1,
            "maxLength": 60
          },
          "note": {
            "description": "Anything else specific to this count.",
            "type": "string",
            "minLength": 1,
            "maxLength": 120
          }
        }
      }
    },
    "scoring_table": {
      "description": "Point values as a lookup, for games where scoring is something you consult mid-hand rather than remember: Cribbage's combinations, Canasta's card values, Pinochle's melds. The prose in goal_and_scoring still explains how scoring works; this is only the numbers.",
      "type": "array",
      "minItems": 2,
      "maxItems": 24,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "item",
          "value"
        ],
        "properties": {
          "item": {
            "description": "What scores, e.g. \"Fifteen\" or \"Each ace\".",
            "type": "string",
            "minLength": 1,
            "maxLength": 60
          },
          "value": {
            "description": "What it is worth, as text so ranges and conditions fit: \"2\", \"-10\", \"20 each\".",
            "type": "string",
            "minLength": 1,
            "maxLength": 40
          },
          "note": {
            "description": "A short qualifier, e.g. \"only if it takes a trick\".",
            "type": "string",
            "minLength": 1,
            "maxLength": 120
          }
        }
      }
    },
    "variants": {
      "description": "A few well-known variations. Not intended to be exhaustive.",
      "type": "array",
      "minItems": 2,
      "maxItems": 8,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name",
          "description"
        ],
        "properties": {
          "name": {
            "type": "string",
            "minLength": 2,
            "maxLength": 80
          },
          "description": {
            "type": "string",
            "minLength": 40,
            "maxLength": 1200
          },
          "players": {
            "description": "The player range this variant serves, when it differs from the game's own. Omit unless the variant's prose states a count plainly -- an absent range costs a missed recommendation, a wrong one costs a reader a game that does not work at their table.",
            "type": "object",
            "additionalProperties": false,
            "required": [
              "min",
              "max"
            ],
            "properties": {
              "min": {
                "type": "integer",
                "minimum": 1,
                "maximum": 12
              },
              "max": {
                "type": "integer",
                "minimum": 1,
                "maximum": 12
              }
            }
          }
        }
      }
    },
    "difficulty": {
      "description": "How much there is to learn before a first game, not how hard it is to play well.",
      "type": "string",
      "enum": [
        "simple",
        "easy",
        "medium",
        "complex"
      ]
    },
    "duration_minutes": {
      "description": "Rough playing time as a range, e.g. \"15-30\", or an open-ended floor, e.g. \"60+\".",
      "type": "string",
      "pattern": "^[0-9]{1,3}-[0-9]{1,3}$|^[0-9]{1,3}\\+$"
    },
    "tags": {
      "description": "Facets for filtering. Drawn from a controlled vocabulary so search stays consistent across the collection.",
      "type": "array",
      "minItems": 1,
      "maxItems": 10,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": [
          "family-friendly",
          "kids",
          "party",
          "partnership",
          "solo",
          "two-player",
          "large-group",
          "quick",
          "long-game",
          "bluffing",
          "memory",
          "strategy",
          "luck",
          "speed",
          "reflex",
          "counting",
          "betting",
          "beginner-friendly",
          "classic",
          "travel",
          "no-table-needed"
        ]
      }
    },
    "sources_consulted": {
      "description": "Names of sources checked while verifying the rules. Attribution as good practice; the write-up itself is original.",
      "type": "array",
      "minItems": 1,
      "maxItems": 12,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 2,
        "maxLength": 120
      }
    },
    "checked": {
      "description": "When this entry's prose was last read against its sources, and a fingerprint of the prose as it stood at that moment. The fingerprint is what makes the record mean anything: edit any of the prose fields afterwards and the validator reports the entry as changed since it was checked, rather than letting a date sit there implying a check that no longer covers the text. Absent means never checked, which is not the same as checked and clean.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "date",
        "prose"
      ],
      "properties": {
        "date": {
          "description": "ISO date the check was made, e.g. \"2026-08-01\".",
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
        },
        "prose": {
          "description": "Fingerprint of the entry's prose at check time. Which fields that covers is PROSE_FIELDS in the naibi package; naming them here instead is how this description came to claim three of them for a day after a fourth joined.",
          "type": "string",
          "pattern": "^[0-9a-f]{16}$"
        },
        "sources": {
          "description": "Which of `sources_consulted` this entry was actually read against at check time, by name. `sources_consulted` is attribution and lists everything looked at; several entries name sources that were never pulled, so it cannot answer how many sources a check really had. This can. Two is the floor because one source cannot corroborate itself. Absent means the check predates this record and how many sources it had is not known, which is not the same as one.",
          "type": "array",
          "minItems": 2,
          "maxItems": 12,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "minLength": 2,
            "maxLength": 120
          }
        },
        "reworded": {
          "description": "A later, wording-only rewrite of the prose: `date` is when it was changed and `prose` is the fingerprint as it now stands, while `checked.prose` above keeps naming the text that was read against the sources on `checked.date`. Present means the entry's wording has moved since its check and its facts have not — which is what removing a verbatim run does, and the reason restamping with the rewrite's date would be a lie: nobody re-read the sources that day. Absent means the prose is still the prose that was checked. See docs/decisions/0025-a-wording-fix-amends-the-check.md.",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "date",
            "prose"
          ],
          "properties": {
            "date": {
              "description": "ISO date the wording was rewritten, e.g. \"2026-08-15\". Never earlier than `checked.date`.",
              "type": "string",
              "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
            },
            "prose": {
              "description": "Fingerprint of the entry's prose as it now stands. Differs from `checked.prose` by construction: equal fingerprints would record a rewrite that did not happen.",
              "type": "string",
              "pattern": "^[0-9a-f]{16}$"
            }
          }
        }
      }
    }
  }
}
